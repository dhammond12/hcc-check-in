import Logo from './Logo';

/**
 * The primary header (or app bar) of the application. Used to show the logo and
 * any top-level actions such as navigation or user settings.
 */
const Header = () => {
	return (
		<header>
			<Logo />
		</header>
	);
};

export default Header;
